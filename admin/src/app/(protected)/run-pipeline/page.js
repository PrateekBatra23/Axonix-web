export default function RunPipelinePage() {
  return (
    <div>
      <h1 className="text-xl font-semibold mb-2">Run Pipeline</h1>
      <p className="text-sm text-gray-500">
        Manual pipeline triggering isn't available yet — this page is a placeholder until the
        backend supports it.
      </p>
    </div>
  );
}